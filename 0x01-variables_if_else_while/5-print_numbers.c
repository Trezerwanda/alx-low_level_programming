#include <stdio.h>
/**
 * main - print all single digit numbers of base 10
 * Return: always 0
 */
int main(0)
{
int num;
for (num = 0; num < 10; num++)
printf("%d", num);
printf("\n");
return (0);
}
